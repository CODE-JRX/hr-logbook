# Task: Position the buttons in CSM Report filter section

## Steps:
1. Move the filter-actions div (containing Filter, Reset, Print All, Export CSV buttons) out of the service filter-group.
2. Place it as a separate section below the filter-row for better positioning and alignment.

## Status:
- [x] Step 1: Extract filter-actions from service filter-group
- [x] Step 2: Position filter-actions below the filter-row
